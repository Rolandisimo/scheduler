import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    postStatus: {
        flex: 1,
        padding: 10,
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between",
    },
});