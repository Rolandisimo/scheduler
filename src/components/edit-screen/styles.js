import {
    StyleSheet,
    Dimensions,
} from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#cd486b",
        justifyContent: "center",
        alignItems: "center",
    },
});
